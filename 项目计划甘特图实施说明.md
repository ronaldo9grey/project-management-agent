# EPC项目标准计划甘特图 - 实施说明

## 📋 项目计划结构

按万哥需求，已生成完整三阶段项目计划甘特图任务数据：

```
项目计划（3大阶段）
├── 阶段1: 项目前置 (23天) - 蓝色 #3b82f6
│   ├── 1.1 项目方案及概算 (7天)
│   ├── 1.2 方案评审 (3天)
│   ├── 1.3 项目立项 (10天)
│   └── 1.4 EPC合同签订 (10天)
├── 阶段2: 项目推进 (40天) - 橙色 #f59e0b
│   ├── 2.1 图纸设计 (10天)
│   ├── 2.2 图纸/预算审查 (4天)
│   ├── 2.3 招标需求审批 (7天)
│   ├── 2.4 招标启动 (1天)
│   ├── 2.5 技术任务书审查 (3天)
│   ├── 2.6 招标公告 (5天)
│   ├── 2.7 招标资格审查 (1天)
│   ├── 2.8 开标评标 (1天)
│   ├── 2.9 中标通知书 (5天)
│   └── 2.10 采购合同签订 (10天)
└── 阶段3: 项目实施 (约73天) - 绿色 #10b981
    ├── 3.1 设备采购与到货 (34天)
    ├── 3.2 安装调试 (25天)
    ├── 3.3 试运行 (10天)
    └── 3.4 项目验收 (5天)
```

---

## 📅 时间规划

| 阶段 | 起止日期 | 工期 | 任务数 | 备注 |
|------|----------|------|--------|------|
| **阶段1** | 2026-03-01 ~ 2026-03-23 | 23天 | 16个 | 前置工作并行优化 |
| **阶段2** | 2026-03-24 ~ 2026-05-02 | 40天 | 21个 | 推进阶段串行为主 |
| **阶段3** | 2026-05-03 ~ 2026-07-15 | 73天 | 32个 | 按EPC方法论拆分 |
| **总计** | 2026-03-01 ~ 2026-07-15 | 136天 | 69个 | 约4.5个月 |

---

## 🔧 阶段3详细拆分说明（按主流EPC方法论）

### 3.1 设备采购与到货 (34天)
```
3.1.1 设备采购订单确认      (3天)
3.1.2 供应商生产制造跟踪    (20天) - 关键路径
3.1.3 出厂检验(FAT)        (3天)
3.1.4 运输物流安排          (7天) - 可与FAT并行
3.1.5 到货验收              (3天)
3.1.6 仓储管理              (2天)
```

### 3.2 安装调试 (25天)
```
3.2.1 安装准备              (3天)
    ├── 施工方案编制
    └── 安全措施落实
3.2.2 设备安装              (10天)
    ├── 基础施工            (4天)
    ├── 设备就位安装        (4天) - 可与基础并行
    └── 管线连接            (4天) - 可与就位并行
3.2.3 系统调试              (9天)
    ├── 单体调试            (4天)
    ├── 联动调试            (3天)
    └── 系统优化            (2天)
3.2.4 初步验收              (3天)
```

### 3.3 试运行 (10天)
```
3.3.1 试运行准备            (2天)
3.3.2 空载试运行            (2天)
3.3.3 负载试运行            (4天) - 可与数据记录并行
3.3.4 试运行数据记录        (5天) - 覆盖整个试运行期
3.3.5 问题整改              (2天)
```

### 3.4 项目验收 (5天)
```
3.4.1 竣工验收准备          (2天) - 可与资料整理并行
3.4.2 竣工资料整理          (3天)
3.4.3 竣工验收              (1天)
3.4.4 移交生产              (2天) - 可与验收并行
3.4.5 质保期开始            (1天)
```

---

## 📦 交付文件

### 1. project_plan_template.json
- **用途**: 前端甘特图组件直接使用
- **格式**: JSON数组，兼容现有 GanttView.vue 组件
- **包含**: 完整任务层级、日期、进度、状态

### 2. project_plan_template.sql
- **用途**: 直接导入数据库 `project_tasks` 表
- **格式**: PostgreSQL INSERT语句
- **注意**: `project_id` 使用 `'TEMPLATE'` 占位符，需替换为实际项目ID

---

## 🚀 使用方式

### 方式1: 前端直接使用（推荐）

```javascript
// 在 GanttView.vue 中加载模板数据
import projectPlanTemplate from './project_plan_template.json'

// 创建新项目时，复制模板任务
const createProjectFromTemplate = (newProjectId, startDate) => {
  const template = projectPlanTemplate.phases.flatMap(p => p.tasks)
  
  // 计算日期偏移
  const templateStart = new Date('2026-03-01')
  const actualStart = new Date(startDate)
  const dayOffset = Math.floor((actualStart - templateStart) / (1000 * 60 * 60 * 24))
  
  return template.map(task => ({
    ...task,
    id: task.task_id.replace('TEMPLATE', newProjectId),
    project_id: newProjectId,
    start: addDays(task.start, dayOffset),
    end: addDays(task.end, dayOffset)
  }))
}
```

### 方式2: 后端API集成

```python
# 在 backend/app/api/v1/project_gantt.py 中添加
@router.post("/projects/{project_id}/apply-template")
def apply_project_template(project_id: str, start_date: date):
    """应用标准项目计划模板"""
    template_tasks = load_template_tasks()  # 从JSON加载
    
    # 计算日期偏移
    template_start = date(2026, 3, 1)
    day_offset = (start_date - template_start).days
    
    # 创建新任务
    for task in template_tasks:
        new_task = ProjectTask(
            task_id=task['task_id'].replace('TEMPLATE', project_id),
            project_id=project_id,
            task_name=task['task_name'],
            parent_task_id=task.get('parent_id'),
            task_level=task['level'],
            start_date=task['start'] + timedelta(days=day_offset),
            end_date=task['end'] + timedelta(days=day_offset),
            status='未开始',
            progress=0
        )
        db.add(new_task)
    
    db.commit()
    return {"message": "模板应用成功", "task_count": len(template_tasks)}
```

### 方式3: 数据库直接导入

```bash
# 替换项目ID后导入
sed 's/TEMPLATE/YOUR_PROJECT_ID/g' project_plan_template.sql > project_plan_actual.sql
psql -h localhost -U yjydb -d project_cost_tracking -f project_plan_actual.sql
```

---

## 🎨 甘特图展示效果

### 颜色编码
- **蓝色 #3b82f6**: 阶段1-项目前置
- **橙色 #f59e0b**: 阶段2-项目推进
- **绿色 #10b981**: 阶段3-项目实施

### 任务层级
- **Level 1**: 阶段汇总节点（如"阶段1: 项目前置"）
- **Level 2**: 主要任务（如"1.1 项目方案及概算"）
- **Level 3**: 子任务（如"需求调研与分析"）
- **Level 4**: 细分任务（如"施工方案编制"）

---

## ⚙️ 关键路径说明

阶段3（项目实施）的关键路径：
```
设备订单确认 → 供应商生产制造 → 出厂检验(FAT) → 运输物流 → 到货验收
                                                    ↓
安装准备 → 基础施工 → 设备就位 → 管线连接 → 单体调试 → 联动调试 → 系统优化 → 初步验收
                                                    ↓
试运行准备 → 空载试运行 → 负载试运行 → 问题整改
                                                    ↓
竣工验收准备 → 竣工资料整理 → 竣工验收 → 移交生产
```

---

## 📝 后续建议

1. **任务依赖**: 当前为平铺结构，建议增加 `depends_on` 字段实现前置任务约束
2. **里程碑**: 可在关键节点设置 `is_milestone=true`（如合同签订、设备到货、项目验收）
3. **资源分配**: 人员分配字段 `assignee` 已预留，可在创建项目时填充
4. **进度跟踪**: 建议增加 `actual_start` / `actual_end` 字段记录实际执行情况
5. **变更管理**: 阶段3（项目实施）实际工期可能变化，建议支持动态调整

---

*生成时间: 2026-03-24*  
*模板版本: v1.0*  
*适用: EPC工程项目标准计划*
